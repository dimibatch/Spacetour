import { TriPlanetPipe } from './tri-planet.pipe';

describe('TriPlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new TriPlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
